# Дизайн автономной локальной сборки YandexAgetn

**Дата:** 2026-02-24  
**Автор:** Yandex Code Assistant  
**Статус:** Утвержден

## Цель MVP

Создать полностью автономную локальную сборку YandexAgetn, которая:
- Работает полностью из локального кода без зависимости от GitHub openclaw
- Изолирована от глобальной установки OpenClaw (если есть)
- Запускается "из коробки" через двойной клик на `yagent-onboard-ui.command`
- Включает в себя все необходимые компоненты для работы
- Готова к тестированию и доработке локально

## Целевая платформа

- **MVP:** macOS (приоритет)
- **Фаза 2:** Windows
- **Требования:** Node.js 22+, pnpm, Ollama (опционально для NDA-режима)

## Архитектура

### Общая структура

```
YandexAgetn/
├── dist/                      # Собранный код (tsdown)
│   ├── entry.js               # Entry point приложения
│   ├── bundled/               # Bundled plugins
│   └── control-ui/            # Веб-интерфейс (Vite build)
│       ├── index.html
│       └── assets/
├── extensions/                # Локальные расширения
│   └── memory-core/           # Расширенный memory-core плагин
│       ├── index.ts
│       ├── config.ts
│       ├── state.ts
│       └── ...
├── node_modules/              # Все npm зависимости
├── skills/                    # 📦 Bundled Superpowers skills
│   ├── brainstorming/
│   ├── systematic-debugging/
│   ├── writing-plans/
│   ├── executing-plans/
│   ├── test-driven-development/
│   ├── verification-before-completion/
│   └── ... (все из Superpowers)
├── src/                       # Исходный TypeScript (для разработки)
├── ui/                        # Исходники UI (для разработки)
├── scripts/                   # Служебные скрипты
│   ├── run-node.mjs           # Локальный запуск
│   ├── ui.js                  # Сборка UI
│   ├── sync-skills.sh         # 🆕 Синхронизация skills
│   └── test-autonomous-setup.sh  # 🆕 Тестирование изоляции
├── test/                      # Тесты
│   ├── integration/           # 🆕 Integration тесты
│   └── e2e/                   # 🆕 E2E тесты
├── openclaw.mjs               # Локальный entry point
├── package.json               # Зависимости проекта
├── pnpm-lock.yaml             # Зафиксированные версии
├── YANDEXAGETN.md             # 🆕 Обновленный README проекта
└── yagent-onboard-ui.command  # 🆕 Улучшенный установщик
```

### Изоляция от глобального OpenClaw

Критически важно гарантировать, что используется локальный код, а не глобальная установка.

**Механизмы изоляции:**

1. **Запуск через явный Node.js:**
```bash
NODE_BIN="$(command -v node)"
"$NODE_BIN" scripts/run-node.mjs --profile yagent gateway ...
```
- НЕ используем команду `openclaw`
- Выполняем через `node` локальный скрипт

2. **Локальный entry point:**
- `scripts/run-node.mjs` → `./openclaw.mjs` → `./dist/entry.js`
- Весь путь загрузки относителен к `$ROOT_DIR` проекта
- Никаких `require('openclaw')` из глобальных node_modules

3. **Явное указание BUNDLED_PLUGINS_DIR:**
```bash
export OPENCLAW_BUNDLED_PLUGINS_DIR="$ROOT_DIR/extensions"
```
- Переменная окружения переопределяет дефолтное поведение
- Система загрузит extensions только из локальной папки
- Не используется `/opt/homebrew/lib/node_modules/openclaw/extensions`

4. **Изолированный STATE_DIR:**
```bash
export OPENCLAW_STATE_DIR="$HOME/.YA-yagent"
```
- Отдельный профиль, не `~/.openclaw`
- Не пересекается с глобальной установкой OpenClaw
- Все конфиги и состояние хранятся отдельно

## Возможности агента

Агент YA имеет три уровня возможностей:

### 1. Встроенные инструменты (из dist/)

Базовые возможности, скомпилированные в код:
- 📂 Работа с файлами (read, write, search, list)
- 💻 Выполнение команд в терминале
- 🌐 Открытие браузера и работа с веб-страницами
- 📝 Поиск по коду и определениям
- 💬 Работа с чатом и сообщениями
- 🔧 Apply diff, insert content, search and replace

→ Все это работает "из коробки" без дополнительных настроек

### 2. Superpowers Skills (из ./skills/)

Методологии и подходы к решению задач:
- 💡 **brainstorming** - планирование и дизайн перед имплементацией
- 🪲 **systematic-debugging** - систематическая отладка
- 📋 **writing-plans** - создание детальных планов имплементации
- ⚡ **executing-plans** - выполнение планов пошагово
- 🧪 **test-driven-development** - разработка через тесты
- ✅ **verification-before-completion** - проверка перед завершением
- 🌳 **using-git-worktrees** - работа с git worktrees
- 👥 **requesting-code-review** - запрос код-ревью
- 📝 **writing-skills** - создание новых skills

→ Эти skills определяют КАК агент подходит к задачам

### 3. MCP Серверы (внешние интеграции)

**Работающие в MVP (автономные):**
- 🔎 **one-search** - веб-поиск через DuckDuckGo
  - Запускается через `npx -y one-search-mcp`
  - Не требует API ключей или настройки
  - Работает сразу "из коробки"
  - Настраивается автоматически при первом запуске

**Пока не работают (требуют корпоративную инфраструктуру):**
- 🔍 **intrasearch** - поиск по вики Яндекса (требует авторизацию)
- 📊 **google-sheets** - работа с таблицами (OAuth токены)
- 📅 **yandex-calendar** - календарь (корпоративная сеть)
- 📈 **datalens** - дашборды (VPN/авторизация)
- 📋 **tracker_mcp** - Яндекс Трекер (внутренний)
- 💾 **yt** - работа с YT (внутренний)

→ Яндекс MCP серверы будут подключены в фазе 2 после настройки инфраструктуры

### Как агент выбирает инструменты

1. Видит задачу пользователя
2. Анализирует доступные skills (например, brainstorming для планирования)
3. Использует встроенные инструменты (файлы, терминал, браузер)
4. При необходимости вызывает MCP сервер (поиск в интернете через one-search)
5. Выполняет задачу пошагово, комбинируя все доступные возможности

## Управление зависимостями

### Стратегия для MVP

**Package.json с зафиксированными версиями:**
```json
{
  "name": "openclaw",
  "version": "2026.2.9",
  "dependencies": {
    "@agentclientprotocol/sdk": "0.14.1",
    "grammy": "^1.40.0",
    "express": "^5.2.1",
    // ... все остальные ~50 зависимостей
  }
}
```

**pnpm-lock.yaml для воспроизводимости:**
- Гарантирует одинаковые версии при каждой установке
- Защита от breaking changes в зависимостях
- Воспроизводимые сборки

**node_modules/ на локальной машине:**
- Для MVP: НЕ коммитим в git (слишком большой размер)
- Устанавливаются через `pnpm install` при сборке
- ~700MB, ~50-60 пакетов

**Фаза 2 (для Аркадии):**
- Оценить размер после оптимизации
- Возможные варианты: архив, vendoring, или dynamic install через npm.yandex-team.ru

### Внешние зависимости (не npm)

**Ollama (опционально):**
- Нужна для NDA-режима с локальной моделью
- Устанавливается отдельно пользователем
- Скрипт проверяет наличие и предлагает установить

**QMD CLI (опционально):**
- Нужна для memory_search и memory_get
- Устанавливается через `npm install -g @tobilu/qmd`
- Скрипт проверяет и предлагает установить

## Процесс сборки

### Для разработчика (однократно)

```bash
cd Projects/YandexAgetn

# 1. Установить зависимости
pnpm install

# 2. Скопировать Superpowers skills
cp -r ../superpowers/skills ./skills/

# 3. Собрать проект
pnpm build

# Результат:
# - dist/ собран
# - dist/control-ui/ собран
# - extensions/ готовы
# - skills/ скопированы
# Проект готов к запуску!
```

### Автоматизация сборки

Создать скрипт `scripts/build-autonomous.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail

echo "Building autonomous YandexAgetn bundle..."

# 1. Install dependencies
pnpm install

# 2. Sync skills from Superpowers
if [[ -d "../superpowers/skills" ]]; then
  echo "Syncing skills from Superpowers..."
  rm -rf ./skills
  cp -r ../superpowers/skills ./skills/
else
  echo "⚠️ Superpowers not found, skills not synced"
fi

# 3. Build project
pnpm build

echo "✓ Build complete!"
echo "Run: ./yagent-onboard-ui.command"
```

### Для пользователя (запуск)

```bash
# Вариант 1: Двойной клик
# (открыть в Finder и дважды кликнуть)
yagent-onboard-ui.command

# Вариант 2: Из терминала
cd Projects/YandexAgetn
./yagent-onboard-ui.command
```

Скрипт автоматически:
1. Проверяет наличие Ollama
2. Проверяет наличие QMD CLI
3. Создает конфигурацию
4. Запускает gateway
5. Открывает браузер с онбордингом

## Конфигурация

### Начальный конфиг (минимальный для совместимости)

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "openrouter/moonshotai/kimi-k2.5"
      },
      "maxConcurrent": 4,
      "subagents": {
        "maxConcurrent": 8
      },
      "thinkingDefault": "high",
      "verboseDefault": "on"
    },
    "list": [
      {
        "id": "main",
        "name": "Обычный",
        "workspace": "$WORKSPACE_BASE/main"
      },
      {
        "id": "nda",
        "name": "NDA",
        "workspace": "$WORKSPACE_BASE/nda",
        "model": {
          "primary": "ollama/qwen2.5:7b-instruct",
          "fallbacks": ["openrouter/moonshotai/kimi-k2.5"]
        }
      }
    ]
  },
  "skills": {
    "allowBundled": ["*"],
    "load": {
      "extraDirs": ["./skills"]  
    }
  },
  "mcpServers": {
    "one-search": {
      "command": "npx",
      "args": ["-y", "one-search-mcp"],
      "env": {
        "SEARCH_PROVIDER": "duckduckgo"
      }
    }
  },
  "plugins": {
    "slots": {
      "memory": "memory-core"
    },
    "entries": {
      "memory-core": {
        "config": {
          "timezone": "Europe/Moscow",
          "language": "ru"
        }
      }
    }
  },
  "memory": {
    "backend": "qmd",
    "qmd": {
      "includeDefaultMemory": false,
      "paths": [
        {
          "path": "$WORKSPACE_BASE/main/memory/recent",
          "name": "memory-recent",
          "pattern": "**/*.md"
        },
        {
          "path": "$WORKSPACE_BASE/main",
          "name": "memory-root",
          "pattern": "MEMORY.md"
        }
      ],
      "update": {
        "interval": "5m"
      }
    }
  }
}
```

**Ключевые моменты:**
- Минимальная конфигурация `memory-core` для совместимости с текущей версией
- Относительный путь к skills: `"./skills"`
- Автоматическая настройка `one-search` MCP сервера
- Изолированные workspace для каждого агента

## Стратегия тестирования

### 1. Ручное тестирование базового функционала

- ✅ Запуск через `yagent-onboard-ui.command`
- ✅ Онбординг открывается в браузере
- ✅ Подключение к Telegram работает
- ✅ Создание задач и отправка сообщений
- ✅ Работа с файлами (read, write, search)
- ✅ Выполнение команд в терминале
- ✅ Открытие браузера и взаимодействие

### 2. Тестирование Superpowers Skills

- ✅ `brainstorming` skill активируется перед творческими задачами
- ✅ `systematic-debugging` работает при отладке
- ✅ `writing-plans` и `executing-plans` выполняются корректно
- ✅ Проверить список доступных skills в bootstrap промпте

### 3. Тестирование изоляции от глобального OpenClaw

- ✅ Код работает без установленного глобального openclaw
- ✅ Код работает С установленным глобальным openclaw (не конфликтует)
- ✅ Extensions загружаются из локальной папки `./extensions`
- ✅ Конфиг создается в `~/.YA-yagent/`, а не в `~/.openclaw`
- ✅ Проверить `OPENCLAW_BUNDLED_PLUGINS_DIR` в процессе

### 4. Smoke тесты

- Чистая установка на новом Mac (или свежем user account)
- Проверка всех зависимостей (Node.js, pnpm)
- Автоматическая установка Ollama при необходимости
- Работа NDA-режима с локальной моделью Qwen 2.5

### 5. Автоматизированные тесты (новые)

**Unit тесты (существующие):**
```bash
pnpm test
```
- Тесты для memory-core (config.test.ts, promote.test.ts, state.test.ts)
- Валидация конфигурации
- Проверка логики компонентов

**Integration тесты (новые):**
```typescript
// test/integration/autonomous-setup.test.ts
describe('Autonomous setup', () => {
  it('loads extensions from local directory', () => {
    // Проверка OPENCLAW_BUNDLED_PLUGINS_DIR
  });
  
  it('uses isolated state directory', () => {
    // Проверка OPENCLAW_STATE_DIR
  });
  
  it('loads skills from bundled directory', () => {
    // Проверка загрузки skills из ./skills
  });
});
```

**E2E тесты (новые):**
```typescript
// test/e2e/onboarding.test.ts
describe('Onboarding flow', () => {
  it('starts gateway successfully', () => {
    // Запуск gateway
  });
  
  it('opens UI in browser', () => {
    // Проверка открытия браузера
  });
  
  it('validates initial config', () => {
    // Проверка корректности конфига
  });
  
  it('one-search MCP is available', () => {
    // Проверка доступности MCP
  });
});
```

**CI/CD скрипт (новый):**
```bash
# scripts/test-autonomous-setup.sh
#!/usr/bin/env bash

# 1. Очистка окружения
rm -rf ~/.YA-yagent
rm -rf ./dist

# 2. Сборка проекта
pnpm install
pnpm build

# 3. Запуск unit тестов
pnpm test

# 4. Запуск integration тестов
pnpm test:integration

# 5. Запуск E2E тестов
pnpm test:e2e

# 6. Проверка изоляции
./scripts/verify-isolation.sh
```

## Критерии готовности MVP

- [ ] Успешный запуск на чистой системе (без глобального OpenClaw)
- [ ] Успешный запуск при наличии глобального OpenClaw (без конфликтов)
- [ ] Все базовые инструменты работают
- [ ] Superpowers Skills активируются и выполняются корректно
- [ ] MCP one-search возвращает результаты поиска
- [ ] Онбординг проходит успешно в браузере
- [ ] Telegram подключение работает
- [ ] NDA-режим с локальной моделью функционирует
- [ ] Все автоматизированные тесты проходят

## Документация

### 1. Обновить YANDEXAGETN.md

Добавить секции:
- **Локальная сборка и запуск** - инструкция для разработчиков
- **Автономная архитектура** - описание изоляции от внешних зависимостей
- **Возможности агента** - три уровня: встроенные, Skills, MCP
- **Требования** - Node.js 22+, pnpm, Ollama (опционально)
- **Troubleshooting** - частые проблемы и решения

### 2. Создать design doc (этот файл)

Сохранен как: `docs/plans/2026-02-24-autonomous-local-setup-design.md`

### 3. Обновить Bootstrap промпт агента

Добавить в системный промпт агента:
```markdown
## Доступные Superpowers Skills

Вы имеете доступ к следующим skills для решения задач:
- brainstorming - используйте ПЕРЕД любой творческой работой
- systematic-debugging - для отладки проблем
- writing-plans - создание детальных планов
- executing-plans - пошаговое выполнение планов
[... полный список с описаниями]

## Система памяти

- memory-core plugin с QMD backend
- Автоматическое сохранение контекста в daily logs
- Поиск по памяти через memory_search
- Долговременная память в MEMORY.md
```

## Trade-offs принятых решений

### ✅ Что выиграли

1. **Полная автономность** - работает без внешних репозиториев
2. **Изоляция** - не конфликтует с глобальным OpenClaw
3. **Bundled Skills** - все методологии включены
4. **Простота запуска** - двойной клик
5. **Контролируемая версия** - зафиксированные зависимости

### ⚠️ Trade-offs

1. **Размер проекта** - ~700MB с node_modules (для Аркадии будет решаться отдельно)
2. **Синхронизация Skills** - нужно обновлять вручную из Superpowers
3. **Сборка** - требуется при каждом изменении кода
4. **Зависимость от npm** - при первой установке нужен доступ к registry

## Следующие шаги

1. ✅ Дизайн утвержден и задокументирован
2. → Передать в `writing-plans` skill для создания детального плана
3. → Имплементация через `executing-plans` skill
4. → Тестирование по чек-листу выше
5. → Подготовка к развертыванию в корпоративной среде (фаза 2)

## Вопросы для фазы 2 (после MVP)

- Как оптимально разместить в Аркадии с учетом размера?
- Использовать ли npm.yandex-team.ru для зависимостей?
- Настройка Яндекс MCP серверов в корпоративной сети
- Процесс обновления для пользователей
- Windows версия установщика